# Cloudflare Workers — set via wrangler secret or dashboard
JWT_SECRET=your-jwt-secret-here
EVENT_BUS_URL=https://event-bus.webwaka.workers.dev
EVENT_BUS_TOKEN=your-event-bus-token-here

# D1 Database — configured in wrangler.toml
# DB binding is injected automatically by Cloudflare Workers

# R2 Storage — configured in wrangler.toml
# STORAGE binding is injected automatically by Cloudflare Workers
